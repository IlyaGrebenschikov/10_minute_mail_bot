version: '3'

networks:
  bot-network:

services:
  bot:
    build: .
    volumes:
      - ..:/10_minute_mail_bot
    command: bash -c 'python -m src.main'
    networks:
      - bot-network
    ports:
      - '8080:8080'
    env_file:
      - .env